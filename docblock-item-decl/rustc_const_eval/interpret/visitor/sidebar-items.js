window.SIDEBAR_ITEMS = {"macro":[["make_value_visitor",""]],"trait":[["MutValueVisitor",""],["Value","A thing that we can project into, and that has a layout. This wouldn’t have to depend on `Machine` but with the current type inference, that’s just more convenient to work with (avoids repeating all the `Machine` bounds)."],["ValueMut","A thing that we can project into given mutable access to `ecx`, and that has a layout. This wouldn’t have to depend on `Machine` but with the current type inference, that’s just more convenient to work with (avoids repeating all the `Machine` bounds)."],["ValueVisitor",""]]};