window.SIDEBAR_ITEMS = {"fn":[["expr_requires_semi_to_be_stmt","Does this expression require a semicolon to be treated as a statement? The negation of this: ‘can this expression be used as a statement without a semicolon’ – is used as an early-bail-out in the parser so that, for instance, if true {…} else {…} |x| 5 isn’t parsed as (if true {…} else {…} | x) | 5"],["expr_trailing_brace","If an expression ends with `}`, returns the innermost expression ending in the `}`"]]};